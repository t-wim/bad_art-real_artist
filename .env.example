# Public API base URL for client-side calls (safe to expose)
NEXT_PUBLIC_API_BASE=https://api.example.com

# Server-side base URL for the upstream API (not exposed to browsers)
API_SERVER_BASE=https://api.internal.example.com

# Secret key used for authenticated upstream requests (keep safe)
API_KEY=changeme
